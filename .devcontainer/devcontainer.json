{
  "name": "postgres_Data",
  "image": "mcr.microsoft.com/devcontainers/universal:2",
  "features": {},
  "forwardPorts": [5432],
  "postCreateCommand": "apt-get update && apt-get install -y docker-compose && docker-compose up -d"
}
